# АРИФМЕТИЧЕСКИЙ КАЛЬКУЛЯТОР

Арифметический калькулятор для Android с поддержкой сессионного хранения вычислений и высокой точностью. В отличие от обычных калькуляторов, это приложение позволяет вводить выражение, сохраняя порядок ввода чисел и операций.

## Особенности и функционал

### Математическая логика
* **Арифметика:** Реализованы базовые операции: сложение, вычитание, умножение и деление.
* **Тригонометрия:** Функции `sin` и `cos` с высокой точностью.
* **Высокая точность:** Использование библиотеки `BigDecimalMath` и типа `BigDecimal` исключает ошибки округления при работе с плавающей точкой.

### Уникальный интерфейс
* **Рекурсивный ввод:** Каждая операция создает новую переменную в стеке, переход к предыдущей с помощью кнопки "=".
* **Стек вычислений:** Отображение всех этапов в виде динамического списка (`RecyclerView`).
* **Обработка ввода:** Поддержка дробной части, инверсия знака (+/-) и защита от некорректных операций (например, деления на ноль).

### 🔹 Инженерные инструменты
* **Режимы угла:** Переключение между градусами (**DEG**) и радианами (**RAD**).
* **Управление вводом:** Посимвольное удаление (**DEL**) и полная очистка сессии (**C**).

## 🛠 Технологический стек
* **Язык:** Java
* **UI:** XML (LinearLayout, RelativeLayout, RecyclerView)
* **Библиотеки:** `ch.obermuhlner:big-math` (для вычислений с произвольной точностью)

## 📸 Скриншоты
<p align="center">
<img src="/screenshots/экран.png" width="300" alt="Главный экран" >
</p>
---
*Developed by Devyaterikov Roman ET-402 в рамках индивидуального проекта по дисциплине программирования мобильных устройств.*
